[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dotenv>=0.9.9",
    "numpy>=2.2.3",
    "pycuda>=2025.1",
    "rich>=13.9.4",
    "websockets>=15.0.1",
    "kafka>=1.3.5"
]

[tool.uv.workspace]
members = ["venv"]
